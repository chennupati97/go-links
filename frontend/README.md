# JumpAlias UI

React + TypeScript + Vite client for the JumpAlias API.

## Scripts

```bash
npm install
npm run dev
npm run build
```

Configure the API origin with `VITE_BACKEND_ORIGIN` (see `.env.example`).
